# CLAUDE.md

This directory's agent and contributor guide is **`AGENTS.md`** — one file,
read by every AI tool. It is imported below so Claude Code loads it
automatically. Edit `AGENTS.md`, not this file.

@AGENTS.md
