FC_BASE_URL=https://fc-spawn.bhautik.in
FC_API_KEY=fc_xxxxxxxxxxxxxxxxxxxxxxxx
# SSH gateway hostname (e.g. gw.eu.bhautik.in) — not used by this example directly.
FCSPAWN_GATEWAY=
# Optional: customize the gateway auth token (any secret string).
# If not set, a built-in demo token is used.
OPENCLAW_GATEWAY_TOKEN=fc-openclaw-demo-token
