fc-sandbox-sdk: 0.3.0
transformers: 5.9.0
torch: 2.9.1 (CPU)
model: distilbert-base-uncased-finetuned-sst-2-english (SST-2, POSITIVE/NEGATIVE)
dataset: 80 labeled movie reviews (40 POSITIVE / 40 NEGATIVE), bundled
shards: 4
sandboxes: 4 concurrent (one wave; <= shared 5/5 cap, MAX_CONCURRENCY 4)
fc control plane: https://fc-spawn.bhautik.in (region eu)
captured: 2026-05-29
rootfs: devbox:1
shape: s-2vcpu-2gb
verified end-to-end: yes — bun index.ts on production FC (CPU-only, no GPU);
  4 concurrent sandboxes, 80 reviews in 4×20 shards, 100.0% accuracy (80/80),
  3.54x speedup (sum 3399ms / max 960ms inference), 83.3 items/sec, ~83s one-time
  cold start; all 4 destroyed, leak check clean (out-of-band listSandboxes confirmed
  all 4 in 'destroyed' status, none running)
